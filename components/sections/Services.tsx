import { services } from "@/data/portfolio";
export function Services() { return <section className="services-section" id="servicos"><div className="services-heading" data-reveal><p className="section-kicker"><span /> O QUE EU FAÇO</p><h2>Soluções completas para sua <em>presença digital.</em></h2><p>Da primeira ideia à publicação: direção visual e desenvolvimento trabalhando como uma única disciplina.</p></div><div className="services-mosaic">{services.map((service) => <article key={service.title} className={`service-block ${service.className}`} data-reveal><div className="service-top"><span>{service.index}</span><small>{service.tag}</small></div><h3>{service.title}</h3><p>{service.description}</p><div className="service-arrow" aria-hidden="true">↗</div><div className="service-preview"><i /><i /><i /></div></article>)}</div></section>; }
